# ---------------- Core ----------------
node_modules/

# Build artifacts (use Actions to deploy; don't commit dist/)
dist/
build/
dist-ssr/

# Caches
.cache/
.parcel-cache/
.turbo/
.eslintcache

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / Editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Env files (keep secrets out of git)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Package managers (do NOT ignore lockfiles; keep one consistent lock)
# package-lock.json / pnpm-lock.yaml / yarn.lock should be committed
